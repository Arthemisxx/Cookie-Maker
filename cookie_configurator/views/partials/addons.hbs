<h3>Selected: </h3>
<ul>
    <li>Base: {{cookie.base}} - {{pricify (findPrice allBases cookie.base)}} PLN</li>
    <li>Addons:
    <ul>{{#each cookie.addons}}
        <li>{{this}} - {{pricify (findPrice @root.allAddons this)}} PLN
            {{#unless ../noDeleteBtn}}
                <a href="/configurator/delete-addon/{{this}}" class="delete-buttons">Delete</a>
            {{/unless}}

        </li>
    {{/each}}
    </ul>

</ul>
<p><strong>Total of {{pricify sum}} PLN</strong></p>
